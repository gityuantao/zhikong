# 直控 中转配置示例 —— 复制为 ~/.zhikong/relay.conf(被控端与控制端各放一份,内容必须一致)
#
# 何时需要它:
#   - 不创建此文件、也不设环境变量 = 走局域网(Bonjour 自动发现,无需任何服务器)。
#   - 外网使用时,才需要指向一台你自建的中转(部署见 server/zkrelay.py 与 server/zkrelay.service)。
#
# 格式(单行,空格分隔,2 个字段):
#     中转地址:端口   端到端口令
#
#   字段 1  中转地址:端口   —— 你自建中转的公网地址。
#   字段 2  端到端口令       —— 可选但强烈建议。绝不会发给中转;两端必须完全一致。
#                            不填则退化为「用 6 位远控码派生密钥」——能看到握手的链路窃听者即可解密,形同没加密。
#
# 远控码(配对码)不在此文件:被控端运行时随机生成 / 可刷新,控制端手动输入。
#
# 把下面这行替换成你自己的中转地址,以及一个足够长的高强度随机口令:
relay.example.com:7777  CHANGE_ME_to_a_long_random_shared_secret
