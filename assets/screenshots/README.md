# Screenshots

Drop UI screenshots here and reference them from the project README:

- `role-picker.png` — launch role chooser (控制端 / 被控端)
- `host.png` — Host window: remote code + "允许远程控制" toggle
- `session.png` — Controller viewing the remote screen
